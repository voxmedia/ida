var MediaList = Backbone.Collection.extend({
  model: Media
  // url: config.serverUrl+"/Media"
});


var TimelineList = Backbone.Collection.extend({
  model: Timeline
  // url: config.serverUrl+"/timeline"
});
